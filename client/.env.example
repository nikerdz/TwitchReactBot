# Client build-time environment variables for Vite.
# Use this when the app is deployed as a static site (GitHub Pages, Netlify, etc.)
# Set this to your Render service base URL, without /api/reactions.
# Example: VITE_API_BASE=https://twitchreactbot.onrender.com
VITE_API_BASE=https://your-render-service-url
